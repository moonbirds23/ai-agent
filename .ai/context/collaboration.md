# 双 AI 协作边界

状态：active

根目录 `AGENTS.md` 与 `CLAUDE.md` 中的协作规则具有最高优先级，本文件只提供任务路由，不复制完整规则。

## 默认流程

1. Codex 判断架构、拆分任务、给出验收和风险。
2. 已确认的任务书放入 `.ai/plans/active/`。
3. Claude Code 按任务书实现、测试并回填 `.ai/handoffs/`。
4. Codex 根据实现结果验收，完成后将任务书移入 `.ai/archive/`。

明确的小型修复可以直接执行，但不得顺手扩展范围。同一批文件在同一时间只能有一个执行者。
